<div>
    <h3>{{name}}</h3>
    <div class="intro">
        {{{intro}}}
    </div>
    <div class="alert alert-info">
        {{warning}}
    </div>
    <!-- In the future, we can embed a simplified 3D viewer here or a button to open in browser -->
    <div class="gear-mobile-placeholder">
        <p>This activity provides immersive 3D content.</p>
    </div>
</div>
